import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'picker-v6-issue',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
